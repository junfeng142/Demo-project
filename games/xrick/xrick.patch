diff -uprN a/build_opk.sh b/build_opk.sh
--- a/build_opk.sh	1970-01-01 08:00:00.000000000 +0800
+++ b/build_opk.sh	2026-03-08 19:02:20.954056772 +0800
@@ -0,0 +1,6 @@
+#!/bin/bash
+
+make
+/opt/mmiyoo/bin/arm-linux-gnueabihf-strip xrick.dge -o _opk/xrick
+mv data.zip _opk/
+mksquashfs _opk/* ../_opk/xrick_funkey-s.opk -all-root -no-xattrs -noappend -no-exports
diff -uprN a/Makefile b/Makefile
--- a/Makefile	2026-02-16 12:09:30.906634223 +0800
+++ b/Makefile	2026-02-16 12:13:48.816635562 +0800
@@ -15,7 +15,7 @@
 # Vars
 #
 
-SDLVERSION=$(shell /opt/miyoo/arm-miyoo-linux-uclibcgnueabi/sysroot/usr/bin/sdl-config --version 2>/dev/null)
+SDLVERSION=$(shell /opt/mmiyoo/arm-buildroot-linux-gnueabihf/sysroot/usr/bin/sdl-config --version 2>/dev/null)
 ROOTDIR=
 #$(shell pwd)
 TARGET=$(shell uname -s | tr [a-z] [A-Z])
@@ -83,10 +83,10 @@ endif
 all:
 	@echo "ROOTDIR=" > Makefile.global
 	@echo "XOBJ=$(XOBJ)" >> Makefile.global
-	@echo "CFLAGS=-g -ansi -pedantic -Wall -W -O2 -I $(ROOTDIR)/include $(shell /opt/miyoo/arm-miyoo-linux-uclibcgnueabi/sysroot/usr/bin/sdl-config --cflags) $(OPTIMISE)" >> Makefile.global
-	@echo "LDFLAGS=-lz $(shell /opt/miyoo/arm-miyoo-linux-uclibcgnueabi/sysroot/usr/bin/sdl-config --libs) -fprofile-arcs" >> Makefile.global
-	@echo "CC=arm-miyoo-linux-uclibcgnueabi-gcc" >> Makefile.global
-	@echo "CPP=arm-miyoo-linux-uclibcgnueabi-gcc -E" >> Makefile.global
+	@echo "CFLAGS=-g -ansi -pedantic -Wall -W -O2 -I $(ROOTDIR)/include $(shell /opt/mmiyoo/arm-buildroot-linux-gnueabihf/sysroot/usr/bin/sdl-config --cflags) $(OPTIMISE)" >> Makefile.global
+	@echo "LDFLAGS=-lz $(shell /opt/mmiyoo/arm-buildroot-linux-gnueabihf/sysroot/usr/bin/sdl-config --libs) -fprofile-arcs" >> Makefile.global
+	@echo "CC=/opt/mmiyoo/bin/arm-linux-gnueabihf-gcc" >> Makefile.global
+	@echo "CPP=/opt/mmiyoo/bin/arm-linux-gnueabihf-gcc -E" >> Makefile.global
 	$(MAKE) -C src all
 
 clean:
diff -uprN a/src/sysevt.c b/src/sysevt.c
--- a/src/sysevt.c	2026-02-16 12:11:35.686634871 +0800
+++ b/src/sysevt.c	2026-02-16 12:35:06.966642194 +0800
@@ -47,35 +47,35 @@ processEvent()	/* #dingoo-a320 - keys re
   switch (event.type) {
   case SDL_KEYDOWN:
     key = event.key.keysym.sym;
-    if (key == syskbd_up || key == SDLK_UP) {
+    if (key == syskbd_up || key == SDLK_UP) { 
       SETBIT(control_status, CONTROL_UP);
       control_last = CONTROL_UP;
     }
-    else if (key == syskbd_down || key == SDLK_DOWN) {
+    else if (key == syskbd_down || key == SDLK_DOWN) { 
       SETBIT(control_status, CONTROL_DOWN);
       control_last = CONTROL_DOWN;
     }
-    else if (key == syskbd_left || key == SDLK_LEFT) {
+    else if (key == syskbd_left || key == SDLK_LEFT) { 
       SETBIT(control_status, CONTROL_LEFT);
       control_last = CONTROL_LEFT;
     }
-    else if (key == syskbd_right || key == SDLK_RIGHT) {
+    else if (key == syskbd_right || key == SDLK_RIGHT) { 
       SETBIT(control_status, CONTROL_RIGHT);
       control_last = CONTROL_RIGHT;
     }
-    else if (key == SDLK_BACKSPACE) {
+    else if (key == SDLK_LSHIFT) {
       SETBIT(control_status, CONTROL_PAUSE);
       control_last = CONTROL_PAUSE;
     }
-    else if (key == SDLK_SPACE || key == SDLK_LSHIFT) {
+    else if (key == SDLK_LALT || key == SDLK_RCTRL) {
       SETBIT(control_status, CONTROL_END);
       control_last = CONTROL_END;
     }
-    else if (key == SDLK_RCTRL) {
+    else if (key == SDLK_ESCAPE) {
       SETBIT(control_status, CONTROL_EXIT);
       control_last = CONTROL_EXIT;
     }
-    else if (key == SDLK_LCTRL || key == SDLK_LALT) {
+    else if (key == SDLK_SPACE || key == SDLK_LCTRL) {
       SETBIT(control_status, CONTROL_FIRE);
       control_last = CONTROL_FIRE;
     }
@@ -89,13 +89,13 @@ processEvent()	/* #dingoo-a320 - keys re
       sysvid_zoom(+1);
     }
 #ifdef ENABLE_SOUND
-    else if (key == SDLK_RETURN) {
+    else if (key == SDLK_RETURN) { 
       syssnd_toggleMute();
     }
-    else if (key == SDLK_LSHIFT) {
+    else if (key == SDLK_BACKSPACE) {
       syssnd_vol(-1);
     }
-    else if (key == SDLK_SPACE) {
+    else if (key == SDLK_TAB) {
       syssnd_vol(+1);
     }
 #endif
@@ -113,35 +113,35 @@ processEvent()	/* #dingoo-a320 - keys re
     break;
   case SDL_KEYUP:
     key = event.key.keysym.sym;
-    if (key == syskbd_up || key == SDLK_UP) {
+    if (key == syskbd_up || key == SDLK_UP) { 
       CLRBIT(control_status, CONTROL_UP);
       control_last = CONTROL_UP;
     }
-    else if (key == syskbd_down || key == SDLK_DOWN) {
+    else if (key == syskbd_down || key == SDLK_DOWN) { 
       CLRBIT(control_status, CONTROL_DOWN);
       control_last = CONTROL_DOWN;
     }
-    else if (key == syskbd_left || key == SDLK_LEFT) {
+    else if (key == syskbd_left || key == SDLK_LEFT) { 
       CLRBIT(control_status, CONTROL_LEFT);
       control_last = CONTROL_LEFT;
     }
-    else if (key == syskbd_right || key == SDLK_RIGHT) {
+    else if (key == syskbd_right || key == SDLK_RIGHT) { 
       CLRBIT(control_status, CONTROL_RIGHT);
       control_last = CONTROL_RIGHT;
     }
-    else if (key == SDLK_BACKSPACE) {
+    else if (key == SDLK_LSHIFT) {
       CLRBIT(control_status, CONTROL_PAUSE);
       control_last = CONTROL_PAUSE;
     }
-    else if (key == SDLK_SPACE || key == SDLK_LSHIFT) {
+    else if (key == SDLK_LALT || key == SDLK_RCTRL) {
       CLRBIT(control_status, CONTROL_END);
       control_last = CONTROL_END;
     }
-    else if (key == SDLK_RCTRL) {
+    else if (key == SDLK_ESCAPE) {
       CLRBIT(control_status, CONTROL_EXIT);
       control_last = CONTROL_EXIT;
     }
-    else if (key == SDLK_LCTRL || key == SDLK_LALT) {
+    else if (key == SDLK_SPACE || key == SDLK_LCTRL) {
       CLRBIT(control_status, CONTROL_FIRE);
       control_last = CONTROL_FIRE;
     }
