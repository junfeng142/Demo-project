diff -uprN a/build.sh b/build.sh
--- a/build.sh	1970-01-01 08:00:00.000000000 +0800
+++ b/build.sh	2026-03-17 15:22:22.482107002 +0800
@@ -0,0 +1,9 @@
+#!/bin/bash
+
+cd lua-5.1.5
+chmod +x ./*.sh
+./build.sh
+cd ../src
+chmod +x ./*.sh
+./build.sh
+cd ..
diff -uprN a/lua-5.1.5/Makefile b/lua-5.1.5/Makefile
--- a/lua-5.1.5/Makefile	2026-03-16 21:40:11.755541564 +0800
+++ b/lua-5.1.5/Makefile	2026-03-16 21:41:17.635541725 +0800
@@ -43,7 +43,7 @@ PLATS= aix ansi bsd freebsd generic linu
 # What to install.
 TO_BIN= lua luac
 TO_INC= lua.h luaconf.h lualib.h lauxlib.h ../etc/lua.hpp
-TO_LIB= liblua.a
+TO_LIB= liblua.a liblua.so
 TO_MAN= lua.1 luac.1
 
 # Lua version and release.
diff -uprN a/lua-5.1.5/build.sh b/lua-5.1.5/build.sh
--- a/lua-5.1.5/build.sh	1970-01-01 08:00:00.000000000 +0800
+++ b/lua-5.1.5/build.sh	2026-03-17 15:30:09.462102630 +0800
@@ -0,0 +1,14 @@
+#!/bin/bash
+
+export PATH=$PATH:/opt/mmiyoo/bin
+export TARGETMACH=arm-linux
+export BUILDMACH=i686-pc-linux-gnu
+export CROSS=arm-linux-gnueabihf
+export CC=${CROSS}-gcc -std=gnu99
+export LD=${CROSS}-ld
+export AS=${CROSS}-as
+export CXX=${CROSS}-g++
+
+make linux
+/opt/mmiyoo/bin/arm-linux-gnueabihf-strip src/liblua.so -o ../_opk/libs/liblua.so
+sudo mv src/liblua.so /opt/mmiyoo/arm-buildroot-linux-gnueabihf/sysroot/usr/lib
diff -uprN a/lua-5.1.5/src/Makefile b/lua-5.1.5/src/Makefile
--- a/lua-5.1.5/src/Makefile	2026-03-16 21:40:28.695541606 +0800
+++ b/lua-5.1.5/src/Makefile	2026-03-17 15:29:18.832103104 +0800
@@ -7,8 +7,8 @@
 # Your platform. See PLATS for possible values.
 PLAT= none
 
-CC= gcc
-CFLAGS= -O2 -Wall $(MYCFLAGS)
+#CC= gcc
+CFLAGS= -O2 -Wall -fPIC $(MYCFLAGS)
 AR= ar rcu
 RANLIB= ranlib
 RM= rm -f
@@ -23,6 +23,7 @@ MYLIBS=
 PLATS= aix ansi bsd freebsd generic linux macosx mingw posix solaris
 
 LUA_A=	liblua.a
+LUA_SO= liblua.so
 CORE_O=	lapi.o lcode.o ldebug.o ldo.o ldump.o lfunc.o lgc.o llex.o lmem.o \
 	lobject.o lopcodes.o lparser.o lstate.o lstring.o ltable.o ltm.o  \
 	lundump.o lvm.o lzio.o
@@ -36,7 +37,7 @@ LUAC_T=	luac
 LUAC_O=	luac.o print.o
 
 ALL_O= $(CORE_O) $(LIB_O) $(LUA_O) $(LUAC_O)
-ALL_T= $(LUA_A) $(LUA_T) $(LUAC_T)
+ALL_T= $(LUA_A) $(LUA_T) $(LUAC_T) $(LUA_SO)
 ALL_A= $(LUA_A)
 
 default: $(PLAT)
@@ -57,6 +58,9 @@ $(LUA_T): $(LUA_O) $(LUA_A)
 $(LUAC_T): $(LUAC_O) $(LUA_A)
 	$(CC) -o $@ $(MYLDFLAGS) $(LUAC_O) $(LUA_A) $(LIBS)
 
+$(LUA_SO): $(CORE_O) $(LIB_O)
+	$(CC) -o $@ -shared $?
+
 clean:
 	$(RM) $(ALL_T) $(ALL_O)
 
diff -uprN a/src/Makefile b/src/Makefile
--- a/src/Makefile	2026-03-17 15:18:08.622109378 +0800
+++ b/src/Makefile	2026-03-17 15:33:37.312100684 +0800
@@ -1,7 +1,7 @@
 DEBUG = 0
 
 ifeq ($(DEBUG),0)
-  CROSS_COMPILE = arm-linux-
+  CROSS_COMPILE = arm-linux-gnueabihf-
 endif
 
 CC = $(CROSS_COMPILE)gcc
@@ -18,6 +18,7 @@ HEADERS = config.h list.h luafun.h
 
 SYSROOT     := $(shell $(CC) --print-sysroot)
 SDL_CFLAGS  := $(shell $(SYSROOT)/usr/bin/sdl-config --cflags)
+SDL_CFLAGS	+= -I"../lua-5.1.5/src/"
 SDL_LIBS    := $(shell $(SYSROOT)/usr/bin/sdl-config --libs)
 
 CFLAGS = ${BASEFLAGS} $(SDL_CFLAGS)
@@ -29,7 +30,7 @@ LDFLAGS = ${BASEFLAGS} $(SDL_LIBS) -lSDL
 ifeq ($(DEBUG),1)
   LDFLAGS += -llua5.1
 else
-  LDFLAGS += -llua
+  LDFLAGS += -llua -ldl
 endif
 #CFLAGS += -DHAS_SDL_MPEG=1 -I/usr/include/smpeg
 #LDFLAGS += -lsmpeg
diff -uprN a/src/build.sh b/src/build.sh
--- a/src/build.sh	1970-01-01 08:00:00.000000000 +0800
+++ b/src/build.sh	2026-03-17 15:22:40.902106829 +0800
@@ -0,0 +1,14 @@
+#!/bin/bash
+
+export PATH=$PATH:/opt/mmiyoo/bin
+export TARGETMACH=arm-linux
+export BUILDMACH=i686-pc-linux-gnu
+export CROSS=arm-linux-gnueabihf
+export CC=${CROSS}-gcc
+export LD=${CROSS}-ld
+export AS=${CROSS}-as
+export CXX=${CROSS}-g++
+
+make clean
+make
+/opt/mmiyoo/bin/arm-linux-gnueabihf-strip jysdllua -o ../_opk/jysdllua
diff -uprN a/src/jymain.c b/src/jymain.c
--- a/src/jymain.c	2026-03-17 15:18:08.692109378 +0800
+++ b/src/jymain.c	2026-03-17 15:18:47.392109015 +0800
@@ -10,6 +10,7 @@
 #include "jymain.h"
 
 // 全程变量
+#define luaL_reg luaL_Reg
 
 SDL_Surface* g_Surface=NULL;    // 游戏使用的视频表面
 SDL_Surface* g_RealSurface=NULL;    // 游戏使用的视频表面
diff -uprN a/src/sdlfun.c b/src/sdlfun.c
--- a/src/sdlfun.c	2026-03-23 09:28:12.055966709 +0800
+++ b/src/sdlfun.c	2026-03-23 09:28:46.325967022 +0800
@@ -177,11 +177,11 @@ int InitGame(void)
 
   if(g_FullScreen == 0)
   {
-    g_RealSurface = SDL_SetVideoMode(320, 240, 16, SDL_HWSURFACE);
+    g_RealSurface = SDL_SetVideoMode(240, 240, 16, SDL_HWSURFACE);
   }
   else
   {
-    g_RealSurface = SDL_SetVideoMode(320, 240, g_ScreenBpp, SDL_HWSURFACE | SDL_DOUBLEBUF | SDL_FULLSCREEN);
+    g_RealSurface = SDL_SetVideoMode(240, 240, g_ScreenBpp, SDL_HWSURFACE | SDL_DOUBLEBUF | SDL_FULLSCREEN);
   }
 
   if(g_RealSurface == NULL) {
