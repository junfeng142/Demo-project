diff -uprN a/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/events/mastermode_port/sdl/native/midp_msgQueue_md.c b/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/events/mastermode_port/sdl/native/midp_msgQueue_md.c
--- a/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/events/mastermode_port/sdl/native/midp_msgQueue_md.c	2026-08-13 19:29:56.063769451 +0800
+++ b/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/events/mastermode_port/sdl/native/midp_msgQueue_md.c	2026-08-13 19:50:05.023762892 +0800
@@ -391,18 +391,7 @@ int KeyboardCheck(SDL_Event *event, Midp
 { int Key = KEYMAP_KEY_INVALID;
   int Physical = event->key.keysym.sym;
   switch(event->key.keysym.sym)
-        { case SDLK_TAB:
-          case SDLK_LCTRL:  Key = KEYMAP_KEY_SOFT1; break;
-          case SDLK_BACKSPACE:
-          case SDLK_ESCAPE:
-          case SDLK_RCTRL:  Key = KEYMAP_KEY_SOFT2; break;
-          case SDLK_UP:     Key = KEYMAP_KEY_UP; break;
-          case SDLK_DOWN:   Key = KEYMAP_KEY_DOWN; break;
-          case SDLK_LEFT:   Key = KEYMAP_KEY_LEFT; break;
-          case SDLK_RIGHT:  Key = KEYMAP_KEY_RIGHT; break;
-          case SDLK_RETURN:
-          case SDLK_SPACE:
-          case SDLK_LALT:   Key = KEYMAP_KEY_SELECT; break;
+        { case SDLK_ESCAPE: Key = KEYMAP_KEY_SOFT2; break;
           case SDLK_CLEAR:
           case SDLK_DELETE: Key = KEYMAP_KEY_CLEAR; break;
           case SDLK_0:      Key = KEYMAP_KEY_0; break;
@@ -444,17 +433,29 @@ int KeyboardCheck(SDL_Event *event, Midp
            * dpad -> directional game actions, A -> Select, side keys ->
            * softkeys, dedicated center button -> 5, lower aux -> star/pound.
            */
+          case SDLK_UP:
           case SDLK_u:      Key = GP2X_Default_keys[0]; break;
+          case SDLK_RIGHT:
           case SDLK_r:      Key = GP2X_Default_keys[6]; break;
+          case SDLK_DOWN:
           case SDLK_d:      Key = GP2X_Default_keys[4]; break;
+          case SDLK_LEFT:
           case SDLK_l:      Key = GP2X_Default_keys[2]; break;
+          case SDLK_RCTRL:
           case SDLK_k:      Key = GP2X_Default_keys[9]; break;
+          case SDLK_RETURN:
           case SDLK_s:      Key = GP2X_Default_keys[8]; break;
+          case SDLK_TAB:
           case SDLK_m:      Key = GP2X_Default_keys[10]; break;
+          case SDLK_BACKSPACE:
           case SDLK_n:      Key = GP2X_Default_keys[11]; break;
+          case SDLK_SPACE:
           case SDLK_a:      Key = GP2X_Default_keys[12]; break;
+          case SDLK_LCTRL:
           case SDLK_b:      Key = GP2X_Default_keys[13]; break;
+          case SDLK_LSHIFT:
           case SDLK_x:      Key = GP2X_Default_keys[14]; break;
+          case SDLK_LALT:
           case SDLK_y:      Key = GP2X_Default_keys[15]; break;
           default: Key = KEYMAP_KEY_INVALID;
         }
diff -uprN a/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/highlevelui/lfjport/sdl/native/lfjport_st_export.c b/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/highlevelui/lfjport/sdl/native/lfjport_st_export.c
--- a/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/highlevelui/lfjport/sdl/native/lfjport_st_export.c	2026-08-13 19:31:45.583768857 +0800
+++ b/phoneme-funkey-clean/phoneME-GP2X-SDL/phoneme_feature/midp/src/highlevelui/lfjport/sdl/native/lfjport_st_export.c	2026-08-13 19:40:54.893765877 +0800
@@ -521,14 +521,14 @@ static int overlay_physical_bind_index(S
     case SDLK_d: case SDLK_DOWN: return 1;
     case SDLK_l: case SDLK_LEFT: return 2;
     case SDLK_r: case SDLK_RIGHT: return 3;
-    case SDLK_a: case SDLK_RETURN: case SDLK_SPACE: return 4;
-    case SDLK_b: return 5;
-    case SDLK_x: return 6;
-    case SDLK_y: return 7;
-    case SDLK_n: return 8;
-    case SDLK_m: return 9;
-    case SDLK_s: return 10;
-    case SDLK_k: return 11;
+    case SDLK_a: case SDLK_SPACE: return 4;
+    case SDLK_b: case SDLK_LCTRL: return 5;
+    case SDLK_x: case SDLK_LSHIFT: return 6;
+    case SDLK_y: case SDLK_LALT: return 7;
+    case SDLK_n: case SDLK_BACKSPACE: return 8;
+    case SDLK_m: case SDLK_TAB: return 9;
+    case SDLK_s: case SDLK_RETURN: return 10;
+    case SDLK_k: case SDLK_RCTRL: return 11;
     default: return -1;
     }
 }
@@ -1218,7 +1218,7 @@ int PhoneMEOverlayHandleSDLEvent(SDL_Eve
         overlay_back();
         return 1;
     }
-    if (key == SDLK_b) {
+    if (key == SDLK_b || key == SDLK_LCTRL) {
         overlay_back();
         return 1;
     }
diff -uprN a/phoneme-funkey-opk-wrapper/package-funkey-opk.sh b/phoneme-funkey-opk-wrapper/package-funkey-opk.sh
--- a/phoneme-funkey-opk-wrapper/package-funkey-opk.sh	2026-08-12 19:38:33.406050888 +0800
+++ b/phoneme-funkey-opk-wrapper/package-funkey-opk.sh	2026-08-12 19:41:33.456049202 +0800
@@ -159,12 +159,12 @@ inject_nokia_stubs() {
     )
 }
 
-if [ -z "$FUNKEY_CC" ] && command -v arm-funkey-linux-musleabihf-gcc >/dev/null 2>&1; then
-    FUNKEY_CC=$(command -v arm-funkey-linux-musleabihf-gcc)
+if [ -z "$FUNKEY_CC" ] && command -v arm-funkey-linux-gnueabihf-gcc >/dev/null 2>&1; then
+    FUNKEY_CC=$(command -v arm-funkey-linux-gnueabihf-gcc)
 fi
 
-if [ -z "$FUNKEY_CC" ] && [ -x "${FUNKEY_SDK_DIR:-/home/pq/FunKey-sdk-2.3.0}/bin/arm-funkey-linux-musleabihf-gcc" ]; then
-    FUNKEY_CC=${FUNKEY_SDK_DIR:-/home/pq/FunKey-sdk-2.3.0}/bin/arm-funkey-linux-musleabihf-gcc
+if [ -z "$FUNKEY_CC" ] && [ -x "${FUNKEY_SDK_DIR:-FunKey-sdk-2.3.0}/bin/arm-funkey-linux-gnueabihf-gcc" ]; then
+    FUNKEY_CC=${FUNKEY_SDK_DIR:-FunKey-sdk-2.3.0}/bin/arm-funkey-linux-gnueabihf-gcc
 fi
 
 if [ ! -x "$MIDP_BIN/runMidlet" ] && [ ! -s "$RUNMIDLET_FALLBACK" ]; then
@@ -293,7 +293,7 @@ cp -R "$MEHOME/build_output_funkey_s/mid
 echo "Compiling SDL launcher (browser mode)"
 
 FUNKEY_SDK_DIR=${FUNKEY_SDK_DIR:-$(dirname "$(dirname "$FUNKEY_CC")")}
-SYSROOT=$FUNKEY_SDK_DIR/arm-funkey-linux-musleabihf/sysroot
+SYSROOT=$FUNKEY_SDK_DIR/arm-funkey-linux-gnueabihf/sysroot
 LAUNCH_SRC=$ROOT/packaging/funkey-s/pm-launch.c
 if [ -f "$LAUNCH_SRC" ]; then
     "$FUNKEY_CC" -Os -s \
diff -uprN a/phoneme-funkey-opk-wrapper/packaging/funkey-s/pm-launch.c b/phoneme-funkey-opk-wrapper/packaging/funkey-s/pm-launch.c
--- a/phoneme-funkey-opk-wrapper/packaging/funkey-s/pm-launch.c	2026-08-12 19:40:09.266049990 +0800
+++ b/phoneme-funkey-opk-wrapper/packaging/funkey-s/pm-launch.c	2026-08-12 20:32:57.046020332 +0800
@@ -28,7 +28,7 @@
 #define BIN_DIR    "/mnt/FunKey/.pm/bin"
 #define RMS_DIR    "/mnt/FunKey/.pm/rms"
 #define FAVORITES_FILE PM_DIR "/favorites.txt"
-static char java_dir[PATH_MAX] = "/mnt/java";
+static char java_dir[PATH_MAX] = "/mnt/JAVA";
 
 /* ── MIDP key codes (from phoneME keymap_input.h) ───── */
 #define K_UP     (-1)
@@ -1019,15 +1019,21 @@ static int funkey_bind_index_from_sdl(in
     case SDLK_r:
     case SDLK_RIGHT:  return 3;
     case SDLK_a:
-    case SDLK_RETURN:
     case SDLK_SPACE:  return 4;
-    case SDLK_b:      return 5;
-    case SDLK_x:      return 6;
-    case SDLK_y:      return 7;
-    case SDLK_m:      return 8;
-    case SDLK_n:      return 9;
-    case SDLK_s:      return 10;
-    case SDLK_k:      return 11;
+    case SDLK_b:
+    case SDLK_LCTRL:  return 5;
+    case SDLK_x:
+    case SDLK_LSHIFT: return 6;
+    case SDLK_y:
+    case SDLK_LALT:   return 7;
+    case SDLK_m:
+    case SDLK_TAB:    return 8;
+    case SDLK_n:
+    case SDLK_BACKSPACE: return 9;
+    case SDLK_s:
+    case SDLK_RETURN: return 10;
+    case SDLK_k:
+    case SDLK_RCTRL:  return 11;
     default:          return -1;
     }
 }
@@ -1669,7 +1675,7 @@ static void handle_key_resolution(SDL_Ke
         if (source_picker_sel >= (int)SOURCE_PRESET_COUNT)
             source_picker_sel = SOURCE_PRESET_COUNT - 1;
         break;
-    case SDLK_b:
+    case SDLK_LCTRL: case SDLK_b:
         /* cancel – go back to browser */
         launching_game_idx = -1;
         state = STATE_BROWSER;
@@ -2037,6 +2043,7 @@ static void handle_key_browser(SDL_Keybo
 
     if (game_count <= 0) {
         switch (kev->keysym.sym) {
+        case SDLK_LCTRL:
         case SDLK_b:
             if (!leave_folder()) scan_games();
             break;
@@ -2067,7 +2074,7 @@ static void handle_key_browser(SDL_Keybo
         game_sel += 5;
         if (game_sel >= game_count) game_sel = game_count - 1;
         break;
-    case SDLK_RETURN: case SDLK_SPACE: case SDLK_a: /* A = open/run */
+    case SDLK_SPACE: case SDLK_a: /* A = open/run */
         if (game_count > 0) {
             if (games[game_sel].is_dir) {
                 enter_selected_folder();
@@ -2087,7 +2094,7 @@ static void handle_key_browser(SDL_Keybo
             }
         }
         break;
-    case SDLK_s: /* START = open/keys */
+    case SDLK_RETURN: case SDLK_s: /* START = open/keys */
         if (game_count > 0) {
             if (games[game_sel].is_dir) {
                 enter_selected_folder();
@@ -2102,7 +2109,7 @@ static void handle_key_browser(SDL_Keybo
             state = STATE_KEYBINDS;
         }
         break;
-    case SDLK_k: /* SELECT = add/remove favorite */
+    case SDLK_RCTRL: case SDLK_k: /* SELECT = add/remove favorite */
         if (game_count > 0 && !games[game_sel].is_dir) {
             if (toggle_favorite(games[game_sel].path)) {
                 if (browsing_favorites) {
@@ -2113,7 +2120,7 @@ static void handle_key_browser(SDL_Keybo
             }
         }
         break;
-    case SDLK_b: /* B = back/refresh */
+    case SDLK_LCTRL: case SDLK_b: /* B = back/refresh */
         if (!leave_folder()) {
             scan_games();
         }
@@ -2269,7 +2276,7 @@ static void handle_key_keybinds(SDL_Keyb
         }
         break;
 
-    case SDLK_b: /* back / cancel capture */
+    case SDLK_LCTRL: case SDLK_b: /* back / cancel capture */
         if (capture_mode) {
             capture_mode = 0;
         } else if (delete_confirm) {
