diff -uprN "a/Chinese (CN)" "b/Chinese (CN)"
--- "a/Chinese (CN)"	1970-01-01 08:00:00.000000000 +0800
+++ "b/Chinese (CN)"	2026-02-27 09:57:19.860809974 +0800
@@ -0,0 +1,94 @@
+Launch an application=启动应用程序
+Configure GMenu2X's options=配置GMenu2X的选项
+Skin=界面主题
+Configure skin=配置界面主题
+Wallpaper=系统壁纸
+Change GMenu2X wallpaper=更换系统壁纸
+Log Viewer=日志阅读器
+Displays last launched program's output=显示最后运行的程序输出日志
+About=关于
+Info about GMenu2X=关于GMenu2X
+Do you want to delete the log file?=您是否想删除此日志文件?
+Yes=是
+No=否
+Select an application=选择想要运行的程序
+Settings=设置
+Language=系统语言
+Set the language used by GMenu2X=设置系统界面语言
+Save last selection=记录最后选择的分组
+Save the last selected link and section on exit=记录最后运行的程序和分组
+Output logs=输出日志
+Logs the output of the links. Use the Log Viewer to read them.=记录并输出日志
+Trim extensions=拓展名设置
+Trim extensions in the file explorer.=文件管理器拓展名设置
+Screen Timeout=屏幕超时
+Set screen's backlight timeout in seconds=设置屏幕背光超时时间（秒）
+Button repeat rate=按钮重复率
+Set button repetitions per second=设置按钮每秒重复次数
+Set the skin used by GMenu2X=为系统选择一个界面主题
+Top Bar=顶部栏颜色
+Color of the top bar=设置顶部栏颜色
+Bottom Bar=底部栏颜色
+Color of the bottom bar=设置底部栏颜色
+Selection=选中项颜色
+Color of the selection and other interface details=当前被选中项目的颜色
+Message Box=对话框颜色
+Background color of the message box=设置对话框的背景颜色
+Message Box Border=对话框边框
+Border color of the message box=设置对话框边框的颜色
+Message Box Selection=对话框中当前被选中的项目
+Color of the selection of the message box=设置对话框中当前被选中项目的颜色
+Title=程序标题
+Link title=设置程序名称
+Description=程序概述
+Link description=设置程序的简要概述
+Section=程序分组
+The section this link belongs to=选择程序所属的分组
+Icon=程序图标
+Select an icon for this link=选择程序的图标
+Manual=程序手册
+Select a manual or README file=选择程序使用手册或说明文档
+Selector Directory=选择程序目录
+Directory to scan for the selector=选择程序默认加载目录(如:游戏rom存放目录)
+Selector Browser=程序浏览器
+Allow the selector to change directory=允许程序选择器改变目录
+Clock frequency=时钟频率
+CPU clock frequency for this link=此程序的CPU时钟频率
+Selector Filter=选择程序筛选器
+Selector filter (Separate values with a comma)=选择程序筛选(用逗号分割)
+Display Console=显示终端
+Must be enabled for console-based applications=基于控制台的应用程序必须启用
+Are you sure?=是否确定?
+Insert a name for the new section=为新分组输入名称
+Up one folder=返回上层目录
+Select=选择
+Confirm=确认
+Exit=退出
+Add section=新建分组
+Delete section=删除分组
+Add link in $1=添加链接 $1
+Show manual of $1=显示$1说明
+Edit $1=编辑 $1
+Delete $1=删除 $1
+Deleting $1=正在删除 $1
+Cancel=取消
+OK=确定
+Backspace=删除
+Space=空格
+Change keys=修改按键
+Change page=换页
+Page=页
+Launching $1=正在运行 $1
+Switch=切换
+Clear=清除
+Decrease=减小
+Increase=增大
+Change value=改变值
+Change color component=改变颜色组合
+Edit=编辑
+no items=目录为空
+Scroll=滚动
+Untitled=无标题
+Wallpaper selection=壁纸选择窗口
+Select a wallpaper from the list=从下列选择一个图片作为壁纸
+Lng=ch
diff -uprN a/build.sh b/build.sh
--- a/build.sh	1970-01-01 08:00:00.000000000 +0800
+++ b/build.sh	2026-02-27 09:14:15.760803649 +0800
@@ -0,0 +1,6 @@
+#!/bin/bash
+
+CMAKE=1
+CMAKE_ARGS="-DCMAKE_TOOLCHAIN_FILE=/opt/FunKey-sdk-2.3.0/share/buildroot/toolchainfile.cmake ${CMAKE_ARGS}"
+cmake $CMAKE_ARGS .
+make
diff -uprN a/buildopts.h.cmakein b/buildopts.h.cmakein
--- a/buildopts.h.cmakein	2026-02-27 09:21:45.970804751 +0800
+++ b/buildopts.h.cmakein	2026-02-27 09:15:12.560803788 +0800
@@ -1,7 +1,7 @@
 #ifndef GMENU2X_BUILDOPTS_H
 #define GMENU2X_BUILDOPTS_H
 
-#define GMENU2X_SYSTEM_DIR "@CMAKE_INSTALL_FULL_DATADIR@/gmenu2x"
+#define GMENU2X_SYSTEM_DIR "/usr/share/gmenu2x"
 #define GMENU2X_CARD_ROOT "@CARD_ROOT@"
 
 #endif /* GMENU2X_BUILDOPTS_H */
diff -uprN a/src/contextmenu.cpp b/src/contextmenu.cpp
--- a/src/contextmenu.cpp	2026-02-27 09:21:45.970804751 +0800
+++ b/src/contextmenu.cpp	2026-02-27 09:16:36.350803993 +0800
@@ -82,7 +82,7 @@ ContextMenu::ContextMenu(GMenu2X &gmenu2
 	for (auto option : options) {
 		w = std::max(w, font.getTextWidth(option->text));
 	}
-	w += 23;
+	w += 80;
 	const int h = (font.getLineSpacing() + 2) * options.size() + 8;
 	box = {
 		static_cast<Sint16>((gmenu2x.width() - w) / 2),
diff -uprN a/src/funkeymenu.cpp b/src/funkeymenu.cpp
--- a/src/funkeymenu.cpp	2026-02-27 09:21:45.970804751 +0800
+++ b/src/funkeymenu.cpp	2026-02-27 13:58:26.280845380 +0800
@@ -20,7 +20,7 @@
 #define MENU_ERROR_PRINTF(...)
 #endif //MENU_ERROR
 
-#define SCREEN_HORIZONTAL_SIZE      240 //RES_HW_SCREEN_HORIZONTAL
+#define SCREEN_HORIZONTAL_SIZE      320 //RES_HW_SCREEN_HORIZONTAL
 #define SCREEN_VERTICAL_SIZE        240 //RES_HW_SCREEN_VERTICAL
 
 #define SCROLL_SPEED_PX             30
@@ -32,11 +32,11 @@
 #define MENU_BG_SQURE_WIDTH         180
 #define MENU_BG_SQUREE_HEIGHT       140
 
-#define MENU_FONT_NAME_TITLE        "/usr/games/menu_resources/OpenSans-Bold.ttf"
+#define MENU_FONT_NAME_TITLE        "/usr/share/fonts/truetype/dejavu/DejaVuSansCondensed-Bold.ttf"
 #define MENU_FONT_SIZE_TITLE        22
-#define MENU_FONT_NAME_INFO         "/usr/games/menu_resources/OpenSans-Bold.ttf"
+#define MENU_FONT_NAME_INFO         "/usr/share/fonts/truetype/dejavu/DejaVuSansCondensed-Bold.ttf"
 #define MENU_FONT_SIZE_INFO         16
-#define MENU_FONT_NAME_SMALL_INFO   "/usr/games/menu_resources/OpenSans-Regular.ttf"
+#define MENU_FONT_NAME_SMALL_INFO   "/usr/share/fonts/truetype/dejavu/DejaVuSansCondensed-Bold.ttf"
 #define MENU_FONT_SIZE_SMALL_INFO   13
 #define MENU_PNG_BG_PATH            "/usr/games/menu_resources/zone_bg.png"
 #define MENU_PNG_ARROW_TOP_PATH     "/usr/games/menu_resources/arrow_top.png"
@@ -53,7 +53,6 @@
 
 #define MAXPATHLEN                  512
 
-
 /// -------------- STATIC VARIABLES --------------
 SDL_Surface * FunkeyMenu::backup_hw_screen = NULL;
 SDL_Surface * FunkeyMenu::draw_screen = NULL;
@@ -394,7 +393,7 @@ void FunkeyMenu::init_menu_zones(){
 	/// Init USB Menu
 	add_menu_zone(MENU_TYPE_USB);
 	/// Init Launcher Menu
-	add_menu_zone(MENU_TYPE_LAUNCHER);
+	//add_menu_zone(MENU_TYPE_LAUNCHER);
 	/// Init Powerdown Menu
 	add_menu_zone(MENU_TYPE_POWERDOWN);
 }
@@ -827,6 +826,8 @@ int FunkeyMenu::launch( )
 							/// ----- Shell cmd ----
 							sprintf(shell_cmd, "%s %d", SHELL_CMD_VOLUME_SET, volume_percentage);
 							system(shell_cmd);
+							// Turn audio amp off to avoid buzzing sound
+							system(SHELL_CMD_AUDIO_AMP_OFF);
 
 							/// ------ Refresh screen ------
 							screen_refresh = 1;
@@ -883,6 +884,8 @@ int FunkeyMenu::launch( )
 							/// ----- Shell cmd ----
 							sprintf(shell_cmd, "%s %d", SHELL_CMD_VOLUME_SET, volume_percentage);
 							system(shell_cmd);
+							// Turn audio amp off to avoid buzzing sound
+							system(SHELL_CMD_AUDIO_AMP_OFF);
 
 							/// ------ Refresh screen ------
 							screen_refresh = 1;
diff -uprN a/src/gmenu2x.cpp b/src/gmenu2x.cpp
--- a/src/gmenu2x.cpp	2026-02-27 09:21:45.970804751 +0800
+++ b/src/gmenu2x.cpp	2026-02-27 13:29:14.910841094 +0800
@@ -71,6 +71,10 @@
 #include <sys/stat.h>
 #include <sys/types.h>
 #include <dirent.h>
+#include <sys/ioctl.h>
+#include <fcntl.h>
+#include <unistd.h>
+#include <linux/fb.h>
 
 #define DEFAULT_FONT_PATH "/usr/share/fonts/truetype/dejavu/DejaVuSansCondensed.ttf"
 #define DEFAULT_FONT_SIZE 12
@@ -326,6 +330,9 @@ GMenu2X::GMenu2X() : input(*this), sc(th
 
     // Init FunkeyMenu
     FunkeyMenu::init( *this );
+	
+	// Turn audio amp off to avoid buzzing sound
+	system(SHELL_CMD_AUDIO_AMP_OFF);
 
 	//powerSaver->setScreenTimeout(confInt["backlightTimeout"]);
 }
@@ -523,6 +530,7 @@ void GMenu2X::readConfig(string conffile
 		confStr["skin"] = "Default";
 
 	evalIntConf( confInt, "outputLogs", 0, 0,1 );
+	evalIntConf( confInt, "trimExt", 0, 0,1);
 	evalIntConf( confInt, "backlightTimeout", 15, 0,120 );
 	evalIntConf( confInt, "buttonRepeatRate", 10, 0, 20 );
 	evalIntConf( confInt, "videoBpp", 32, 16, 32 );
@@ -626,7 +634,7 @@ void GMenu2X::mainLoop() {
 				(!menu->selLinkApp()->getSelectorDir().empty()
 				 || !lastSelectorDir.empty()))
 		menu->selLinkApp()->selector(lastSelectorElement, lastSelectorDir);
-
+    int fd = open("/dev/fb0", O_RDWR);
 	while (true) {
 		// Remove dismissed layers from the stack.
 		for (auto it = layers.begin(); it != layers.end(); ) {
@@ -647,6 +655,7 @@ void GMenu2X::mainLoop() {
 		for (auto layer : layers) {
 			layer->paint(*s);
 		}
+		ioctl(fd, FBIO_WAITFORVSYNC, 0);
 		s->flip();
 
 		// Exit main loop once we have something to launch.
@@ -704,6 +713,7 @@ void GMenu2X::mainLoop() {
 			}
 		}
 	}
+	close(fd);
 }
 
 void GMenu2X::explorer() {
@@ -750,36 +760,40 @@ void GMenu2X::showSettings() {
 			*this, tr["Language"],
 			tr["Set the language used by GMenu2X"],
 			&lang, &translations)));
-	sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingBool(
+	/*sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingBool(
 			*this, tr["Save last selection"],
 			tr["Save the last selected link and section on exit"],
-			&confInt["saveSelection"])));
+			&confInt["saveSelection"])));*/
 	sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingBool(
 			*this, tr["Output logs"],
 			tr["Logs the output of the links. Use the Log Viewer to read them."],
 			&confInt["outputLogs"])));
-	sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingInt(
+	sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingBool(
+			*this, tr["Trim extensions"],
+			tr["Trim extensions in the file explorer."],
+			&confInt["trimExt"])));
+	/*sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingInt(
 			*this, tr["Screen Timeout"],
 			tr["Set screen's backlight timeout in seconds"],
-			&confInt["backlightTimeout"], 0, 120)));
+			&confInt["backlightTimeout"], 0, 120)));*/
 	sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingInt(
 			*this, tr["Button repeat rate"],
 			tr["Set button repetitions per second"],
 			&confInt["buttonRepeatRate"], 0, 20)));
-	if (brightnessmanager->available()) {
+	/*if (brightnessmanager->available()) {
 		sd.addSetting(unique_ptr<MenuSetting>(new MenuSettingInt(
 				*this, tr["Brightness level"],
 				tr["Set the brightness level"],
 				&confInt["brightnessLevel"],
 				1, brightnessmanager->maxBrightness())));
-	}
+	}*/
 
 	if (sd.exec()) {
 		//powerSaver->setScreenTimeout(confInt["backlightTimeout"]);
 
 		input.repeatRateChanged();
-		if (brightnessmanager->available())
-			brightnessmanager->setBrightness(confInt["brightnessLevel"]);
+		/*if (brightnessmanager->available())
+			brightnessmanager->setBrightness(confInt["brightnessLevel"]);*/
 
 		if (lang == "English") lang = "";
 		if (lang != tr.lang()) {
diff -uprN a/src/selector.cpp b/src/selector.cpp
--- a/src/selector.cpp	2026-02-27 09:21:45.970804751 +0800
+++ b/src/selector.cpp	2026-02-27 14:06:17.330846533 +0800
@@ -117,7 +117,7 @@ int Selector::exec(int startSelection) {
 				string path = screendir + trimExtension(fl[selected]) + ".png";
 				auto screenshot = OffscreenSurface::loadImage(path, false);
 				if (screenshot) {
-					screenshot->blitRight(s, gmenu2x.width(), 0, gmenu2x.width(), gmenu2x.height(), 128u);
+					screenshot->blitRight(s, gmenu2x.width(), 25, 170, 190, 255u);
 				}
 			}
 
@@ -142,7 +142,7 @@ int Selector::exec(int startSelection) {
 							x, iY + lineHeight / 2,
 							Font::HAlignLeft, Font::VAlignMiddle);
 				} else {
-					gmenu2x.font->write(s, trimExtension(fl[i]),
+					gmenu2x.font->write(s, (gmenu2x.confInt["trimExt"] ? trimExtension(fl[i]) : fl[i]),
 							x, iY + lineHeight / 2,
 							Font::HAlignLeft, Font::VAlignMiddle);
 				}
